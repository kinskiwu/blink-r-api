import { Request, Response } from 'express';
import { UrlModel } from '../models/urls.model';
import { generateShortUrl } from '../services/helpers';


export const shortenUrl = async(req,res, next) => {

}